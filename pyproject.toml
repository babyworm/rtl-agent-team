[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "slow: marks tests as slow (integration tests)",
]
# Default: run unit tests only (use -m 'not slow' or explicit path)
addopts = "--tb=short -q"
